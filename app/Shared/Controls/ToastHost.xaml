<UserControl
    x:Class="Alpheratz.Shared.Controls.ToastHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid IsHitTestVisible="False">
        <ItemsControl ItemsSource="{Binding toasts}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,14,14,0">
            <ItemsControl.ItemContainerTransitions>
                <TransitionCollection>
                    <AddDeleteThemeTransition/>
                    <EntranceThemeTransition FromHorizontalOffset="40" FromVerticalOffset="0"/>
                </TransitionCollection>
            </ItemsControl.ItemContainerTransitions>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border MinWidth="240" MaxWidth="420" Margin="0,0,0,8" Padding="14,10" CornerRadius="{StaticResource ARadius}" Background="#F216181C" BorderBrush="{ThemeResource ABorderStrong}" BorderThickness="1">
                        <TextBlock Text="{Binding message}" Foreground="White" FontSize="13" TextWrapping="Wrap"/>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
