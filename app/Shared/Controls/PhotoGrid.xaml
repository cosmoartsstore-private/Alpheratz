<UserControl
    x:Class="Alpheratz.Shared.Controls.PhotoGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Alpheratz.Shared.Controls">
    <Grid Background="{ThemeResource ASurface}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="12"/>
        </Grid.ColumnDefinitions>

        <local:PhotoGridItemsView x:Name="ItemsViewControl"
                                  Grid.Column="0"/>

        <local:CustomScrollbar x:Name="CustomScrollbarControl"
                               Grid.Column="1"
                               ThumbTop="0"
                               ThumbHeight="48"/>
    </Grid>
</UserControl>
