<UserControl
    x:Class="Alpheratz.Shared.Controls.FavoriteCornerBadge"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Alpheratz.Shared.Controls"
    x:Name="Root"
    Width="{Binding BadgeSize, ElementName=Root}"
    Height="{Binding BadgeSize, ElementName=Root}">
    <UserControl.Resources>
        <ControlTemplate x:Key="FavoriteCornerBadgeButtonTemplate" TargetType="Button">
            <ContentPresenter Content="{TemplateBinding Content}"
                              ContentTemplate="{TemplateBinding ContentTemplate}"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
        </ControlTemplate>
    </UserControl.Resources>

    <Button x:Name="InteractiveButton"
            Width="{Binding BadgeSize, ElementName=Root}"
            Height="{Binding BadgeSize, ElementName=Root}"
            Padding="0"
            Background="Transparent"
            BorderBrush="Transparent"
            BorderThickness="0"
            Template="{StaticResource FavoriteCornerBadgeButtonTemplate}"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Click="InteractiveButton_Click"
            Tapped="InteractiveButton_Tapped">
        <Grid Width="{Binding BadgeSize, ElementName=Root}"
              Height="{Binding BadgeSize, ElementName=Root}">
            <Path x:Name="Triangle"
                  Data="M0,0 L1,0 L1,1 Z"
                  Stretch="Fill"/>
            <controls:AppIcon x:Name="StarIcon"
                              IconName="star"
                              IconSize="{Binding IconSize, ElementName=Root}"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Top"
                              Margin="0,4,4,0"/>
        </Grid>
    </Button>
</UserControl>
